'use client';

// Placeholder for future Laravel Auth implementation
export function useSession() {
  // TODO: Implement session check with Laravel API
  const session = null;
  const loading = false;

  return { session, loading };
}
